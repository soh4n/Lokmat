# LokMat Backend — FastAPI
# Core
fastapi[standard]==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.0
pydantic-settings==2.5.2

# Gemini AI
google-genai==1.10.0
google-cloud-logging==3.11.0
google-cloud-storage==2.18.0

# Firebase Auth (per GEMINI.md: Firebase Admin SDK for auth)
firebase-admin==6.6.0

# Database — Cloud SQL (PostgreSQL)
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.13.0

# Redis — Memorystore
redis[hiredis]==5.0.0

# Auth (local JWT fallback)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP
httpx>=0.28.1,<1.0.0

# Environment
python-dotenv==1.0.1

# Dev / Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
respx==0.21.0
ruff==0.7.0
mypy==1.11.0
